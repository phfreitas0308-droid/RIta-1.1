# Opcional. Modelo a usar. Se não definido, o código usa "gpt-5.6-terra".
# Confira os modelos disponíveis na sua conta em https://platform.openai.com/docs/models
OPENAI_MODEL=gpt-5.6-terra

# As variáveis abaixo só importam se você rodar scripts/build_index.js
# (geração do índice de busca / RAG). Os valores padrão já funcionam bem —
# só mude se souber o que está fazendo, e rode o script de novo depois.
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# OPENAI_EMBEDDING_DIMENSIONS=512
# RAG_TOP_K=8

# Opcional. Modelo usado só para classificar a pergunta (ambígua? complexa?)
# antes de responder. Um modelo mais barato/rápido é suficiente para essa etapa.
# ANALYZER_MODEL=gpt-5.6-luna

# Opcional. Número máximo de perguntas permitidas por conversa/sessão.
# Padrão: 4. Aplicado no servidor (api/chat.js), então não pode ser burlado
# só desativando o JavaScript do navegador. O frontend (index.html) também
# usa esse mesmo valor (constante MAX_PERGUNTAS_POR_SESSAO no <script>) para
# mostrar o aviso e bloquear o campo de pergunta — se mudar aqui, ajuste lá
# também para os dois ficarem sincronizados.
# MAX_PERGUNTAS_POR_SESSAO=4
